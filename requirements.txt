# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.32        # file upload support
httpx==0.27.2                   # async HTTP client (used by tests + healthchecks)

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.1        # BaseSettings with .env support

# ── LangChain Ecosystem ───────────────────────────────────────────────────────
langchain==0.3.7
langchain-core==0.3.19
langchain-community==0.3.7      # Ollama LLM + ChromaDB integrations
langgraph==0.2.45               # stateful agent workflows
langchain-ollama==0.2.1         # first-party Ollama integration

# ── Vector Store ─────────────────────────────────────────────────────────────
chromadb==0.5.18

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers==3.3.1    # BAAI/bge-large-en-v1.5
torch==2.5.1                    # sentence-transformers backend (CPU build)
transformers==4.46.3

# ── Document Loaders ─────────────────────────────────────────────────────────
pymupdf==1.24.13                # PDF parsing (fitz)
python-docx==1.1.2              # DOCX parsing
beautifulsoup4==4.12.3          # web scraping
lxml==5.3.0                     # HTML/XML parser backend

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1            # .env file loading
tenacity==9.0.0                 # retry logic for LLM calls
tiktoken==0.8.0                 # token counting (model-agnostic)
numpy==1.26.4                   # array ops (pinned for compatibility)

# ── Observability ──────────────────────────────────────────────────────────────
langfuse>=4.0.0                 # LLM observability and tracing

# ── Logging ───────────────────────────────────────────────────────────────────
structlog==24.4.0               # structured JSON logging
rich==13.9.4                    # pretty console output

# ── Security / Middleware ─────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT handling
passlib[bcrypt]==1.7.4            # password hashing
slowapi==0.1.9                    # rate limiting for FastAPI

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2                   # async test client for FastAPI
