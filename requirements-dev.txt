# Development / linting / formatting — not installed in production
-r requirements.txt

ruff==0.7.4                     # fast linter + formatter
mypy==1.13.0                    # static type checking
pre-commit==4.0.1               # git hooks
ipykernel==6.29.5               # Jupyter for RAG experimentation
